HMPV news sentiment Analysis 

First install the extension in vscode that is "Go Live"
Then run the project on vscode on clicking the Go Live in bottom of VS code.
then setup the virtual environment of python 
.venv/Scripts/Activate
Then run python app.py
